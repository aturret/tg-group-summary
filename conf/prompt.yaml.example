# Prompt configuration for Telegram group summary.
# Copy this file to conf/prompt.yaml to override defaults.
#
# All keys are optional. If a key is missing, the hardcoded default is used.
# Environment variables (DECLARATION, ADDITIONAL_PROMPT) take effect only
# when this file is absent or the corresponding key is omitted.

# --- Declaration (footer appended to the report) ---
declaration: >-
  <i>本日报由生成式人工智能创作，仅供各位群友参考，请谨慎甄别内容真实性和有效性。</i> #日报

# --- Additional prompt (extra instructions injected into the prompt) ---
additional_prompt: |
  该群聊有一些特定的用语偏好，请你在总结时注意替换：
  1. 当提及该群聊本身时，请用“斋”代替
  2. 当提及群内进行讨论的用户时，请用“贤达”代替
  3. 如果有用户转发了由生成式人工智能创作的当日聊天记录总结报告，请不要将其纳入你生成的总结报告中。

# --- Media instruction (controls how media content is handled) ---
# Can be a simple string, or a dict with "analyze" and "reference_only" variants.
media_instruction:
  analyze: "包含在 media 字段中的内容需作为重要上下文分析。"
  reference_only: "media 字段仅作参考（如"发送了图片"），无需深入分析媒体内容，除非文本中有上下文提及。"

# --- Prompt template ---
# Available placeholders (filled at runtime):
#   {chat_name}          - Group chat display name
#   {date}               - Date string (e.g. "2026年3月23日")
#   {declaration}        - Resolved from this file's "declaration" key above
#   {additional_prompt}  - Resolved from this file's "additional_prompt" key above
#   {media_instruction}  - Resolved from this file's "media_instruction" key above
#
# Literal braces in the template (e.g. for Telegram link placeholders) must be
# doubled: {{chatId}}, {{messageId}}, {{标题}}, etc.
prompt_template: |
  你是一位专业的舆情分析师和简报编辑。我将提供一份 JSON 格式的 Telegram 聊天记录，请你将其整理为一份高质量的**日报简报**。

  ### 数据说明
  数据是标准的 Telegram 消息对象列表。关键字段：user (发送者), text (内容), replyTo (回复关系), media (媒体文件)。
  请忽略 isBot=true 的机器人消息。
  {media_instruction}

  ### 分析要求
  1. **话题聚类**：将碎片化的聊天内容按主题归类，选取热度最高或价值最高的 5-8 个话题。
  2. **深度挖掘**：对于时政、新闻、行业类话题，需结合 reply 上下文，分析群友的观点倾向、补充的背景信息或对新闻的独特解读。如果涉及不确定的新闻事实，请务必利用工具联网搜索确认。
  3. **去噪**：过滤掉纯粹的闲聊（如打招呼、表情包刷屏），除非它们构成了某种群体性情绪。

  ### 输出格式（严格遵守 HTML）
  请输出一段可以直接通过 Telegram 发送的 HTML 文本。不要使用 Markdown 代码块。格式规范如下：

  第一行标题：<b>{chat_name} {date} 聊天记录总结报告</b>

  对于每个话题，请严格按以下结构输出（注意空行和加粗）：
  1. **话题标题**：必须包含超链接。HTML格式：<a href="https://t.me/c/{{chatId}}/{{messageId}}"><b>{{标题}}</b></a>
     （链接指向该话题的第一条或最具代表性的消息 messageId）
  2. **核心内容**：可以总结成一段文字，也可以根据内容中的子论点或次要内容使用类似"•"的项目符号，分 2-4 点陈述。格式类似于：
     • <b>{{子标题/关键点}}</b>：具体内容的简练描述。

  ### 示例风格
  （参考以下风格，但保持 HTML 标签）
  <a href="..."><b>某某政策引发讨论</b></a>
  • <b>核心变化</b>：群友分享了新规，指出相比旧版主要变化在于...
  • <b>主要争议</b>：部分群友认为这将导致...，而另一方则认为...

  ### 补充指令
  {additional_prompt}
  请在结尾另起一行添加：{declaration}

  现在，请开始分析以下数据：
